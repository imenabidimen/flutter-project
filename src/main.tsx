import React,{useState}from'react';import{createRoot}from'react-dom/client';import{BrowserRouter,useNavigate}from'react-router-dom';import'./styles.css';
function App(){const nav=useNavigate();const[authed,setAuthed]=useState(Boolean(localStorage.getItem('accessToken')));const[customers,setCustomers]=useState([{id:1,name:'Acme Studio',email:'hello@acme.test'},{id:2,name:'Northwind',email:'support@northwind.test'}]);const[query,setQuery]=useState('');const login=()=>{localStorage.setItem('accessToken','demo-token');setAuthed(true)};if(!authed)return <main className="card"><h1>ClientHub</h1><p>Support workspace</p><button onClick={login}>Sign in</button></main>;const filtered=customers.filter(c=>c.name.toLowerCase().includes(query.toLowerCase()));return <main><header><h1>ClientHub</h1><button onClick={()=>{localStorage.removeItem('accessToken');setAuthed(false);nav('/')}}>Log out</button></header><section className="card"><h2>Customers</h2><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search customers"/>{filtered.map(c=><article key={c.id}><strong>{c.name}</strong><span>{c.email}</span></article>)}<button onClick={()=>setCustomers([...customers,{id:Date.now(),name:'New Customer',email:'new@example.test'}])}>Add customer</button></section></main>};createRoot(document.getElementById('root')!).render(<BrowserRouter><App/></BrowserRouter>);